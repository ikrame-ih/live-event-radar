# Copy to .env.local and restart npm run dev after changes.
# NEXT_PUBLIC_* values are visible in the browser — no secrets here.

# WebSocket endpoint. Leave empty to use the mock timer only.
NEXT_PUBLIC_WS_URL=

# true = fake events (setInterval). false + WS_URL set = live socket.
NEXT_PUBLIC_SIMULATOR_ONLY=true
